<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My School <%= title %></title>
    <link rel="stylesheet" href="/style.css">
    <script>
        function toggleNav(){
            document.querySelector('.nav').classList.toggle('nav2');
        }
    </script>
</head>
<body>

    <div class="header">
        <div>
                <img src="/images/lasu-logo.png" alt="">
                <% if(user) { %>
                    <div>
                        <h4><a href="/chat">LASUSTECH Complaint Centre</a></h4>
                        <p>Welcome, <%= user.firstName %> (<%= user.email %>)</p>
                    </div>
                <% }else { %>
                    <div>
                        <h4><a href="/">LASUSTECH Complaint Centre</a></h4>
                    </div>
                <% } %>
        </div>
        <div class="navIcon" onclick="toggleNav()">
            &#9776;
        </div>
        <div class="nav">
            <% if(user) { %>
                <a href="/logout">Logout</a>
            <% }else { %>
                <a href="/signup">Sign up</a>
                <a href="/login">Login</a>
            <% } %>
        </div>
    </div>

    <div class="container">